{
  "name": "tsx-json-bug",
  "version": "1.0.0",
  "description": "Minimal reproduction: tsx v4.21.1 breaks JSON imports (statuses/codes.json)",
  "type": "module",
  "scripts": {
    "test": "node --test-reporter spec  --import=tsx/esm --test 'src/**/*.test.ts'",
    "test:good": "node --test-reporter spec  --import=tsx-good/esm --test 'src/**/*.test.ts'",
    "test:bad": "node --test-reporter spec  --import=tsx-bad/esm  --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "statuses": "^2.0.2"
  },
  "devDependencies": {
    "tsx-bad": "npm:tsx@4.21.1",
    "tsx-good": "npm:tsx@4.21.0",
    "@types/node": "^25.9.1",
    "@types/statuses": "^2.0.6"
  }
}
